{
  "name": "elegant",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:python": "cd python-engine && python -m uvicorn main:app --host 127.0.0.1 --port 8001 --reload",
    "dev": "concurrently -n frontend,backend,python -c cyan,green,yellow \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:python\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
